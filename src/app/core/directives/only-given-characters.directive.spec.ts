import { OnlyGivenCharactersDirective } from './only-given-characters.directive';

describe('OnlyGivenCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyGivenCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
