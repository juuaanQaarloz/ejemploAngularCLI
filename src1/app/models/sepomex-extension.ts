export interface SepomexExtension {
  street: string;
  interiorNumber: string;
  exteriorNumber: string;
  settlement: string; // colonia / barrio
  settlementType: string;
  townHall: string; // municipio / alcaldia
  zoneType: string;
}
